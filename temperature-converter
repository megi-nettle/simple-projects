function TheConverter () {
  var celsius = prompt('What is the temperature outside, in °C?');
  var fahrenheit = celsius * 9 / 5 +32;
  alert('That is ' + fahrenheit + ' degrees Fahrenheit.');
  return; 
};

TheConverter();

/*
function celsiusConverter() {
  var fahrenheit = prompt('Number of Fahrenheit degrees: ');
  var celsius = (fahrenheit - 32) * 5 / 9;
  alert('That is ' + celsius + ' degrees Celsius.');
  return;
};
celsiusConverter();
*/
